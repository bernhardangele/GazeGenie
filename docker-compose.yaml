services:
  dist_app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8501:8501"
    volumes:
      - ./results:/app/results
      - ./testfiles:/app/testfiles
      - ./logs:/app/logs
    environment:
      - PYTHONUNBUFFERED=1

volumes:
  results:
  testfiles:
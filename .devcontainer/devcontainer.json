{
  "name": "GazeGenie App",
  "image": "bangele1/gazegenie:latest",
  "forwardPorts": [8501],
  "mounts": [
    "source=${localWorkspaceFolder}/results,target=/app/results,type=bind"
  ],
  "postAttachCommand": "streamlit run app.py --server.port 8501 --server.address 0.0.0.0",
  "customizations": {
    "vscode": {
      "settings": {},
      "extensions": []
    }
  }
}
